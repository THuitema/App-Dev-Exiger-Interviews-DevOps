# Use Python 3.9 base image

# Set working directory to "/code"

# Copy requirements.txt into working directory and install requirements

# Copy appliation code into working directory

# Command to run: "fastapi run main.py --port 80"
